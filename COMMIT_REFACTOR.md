# 项目迭代记录 - v1.0 到 v2.0 整改历程

> **定位**：从"玩具 Demo"到"全流程实战 Demo"的技术迭代记录
> 
> **核心目标**：体现持续优化、问题解决、技术诚实

---

## v1.0 初始版本（反思）

### 问题诊断

**2026-04-05 自我审视发现的问题**：

1. **虚假定位**
   - 标榜"企业级"、"生产级"、"从零手写"
   - 实际功能简陋，核心技术依赖 LangChain
   - 技术栈堆砌（K8s、Terraform、Prometheus），大多为空架子

2. **面试八股导向**
   - README 写满"面试谈资"、"八股考点"
   - 重点讲"熔断器状态机"，而非实际 RAG 功能
   - 忽略了求职 Demo 的核心：展示完整开发能力

3. **技术栈冗余**
   - Celery + Redis + PostgreSQL：配置复杂，调试困难
   - Prometheus + Grafana：空配置文件，无法演示
   - K8s/Helm/Terraform：仅有目录，无实质内容

4. **核心功能缺失**
   - 文档解析：仅简单分块，无异常处理
   - 向量检索：单一 Dense，精度低
   - 大模型接入：仅包装第三方库
   - 幻觉检测：空字段，未实现

### 技术债务清单

| 模块 | 问题 | 严重程度 |
|------|------|---------|
| 文档处理 | 无格式校验、无乱码处理、无异常捕获 | 🔴 高 |
| 向量检索 | 耦合 Chroma、无抽象层、无混合检索 | 🔴 高 |
| RAG 流程 | 不完整、无 Prompt 工程、无幻觉检测 | 🔴 高 |
| 接口服务 | 接口不完善、无文档、无校验 | 🟡 中 |
| 稳定性 | 单机版吹嘘分布式、重启丢状态 | 🟡 中 |
| 部署 | 空架子配置、无法一键启动 | 🟡 中 |
| 技术栈 | 过度设计、维护成本高 | 🟡 中 |

---

## v2.0 整改版本（实战导向）

### 整改核心原则（2026-04-08 确立）

1. **放弃虚假定位**：明确是"求职实战 Demo"，非企业级
2. **短期快速落地**：2-3 天完成可运行 Demo，单机演示
3. **全流程覆盖**：文档→解析→分块→向量化→检索→生成完整闭环
4. **突出踩坑复盘**：记录真实问题与解决方案，体现能力

### 整改执行记录

#### Day 1：文档整改 + 技术栈精简

**上午：README 彻底重写**
```
- 标题：企业级 RAG 系统 → 大模型 RAG 问答实战 Demo
- 简介：明确单机版、求职项目、功能完整但非生产级
- 删除：生产级、高可用、分布式等虚假描述
- 新增：踩坑记录、技术诚实声明、求职展示建议
```

**下午：技术栈精简**
```
删除：
- ❌ Celery + Redis → 改为 FastAPI 同步任务
- ❌ PostgreSQL → 改为 SQLite
- ❌ Prometheus + Grafana → 改为控制台日志
- ❌ K8s + Helm + Terraform → 改为单机 Docker
- ❌ 冗余依赖（torch 全量、大量可选包）

保留：
- ✅ FastAPI（轻量接口）
- ✅ LangChain（RAG 框架，诚实标注依赖）
- ✅ Milvus Standalone（向量库，单机可跑）
- ✅ BGE-small（Embedding，轻量快速）
- ✅ SQLite（零配置元数据存储）
```

**踩坑记录**：
- 问题：初期选 Celery 导致部署复杂，调试耗时
- 解决：精简为同步任务，聚焦核心 RAG 功能

---

#### Day 2：核心模块整改

**上午：文档处理模块重构**
```
改进点：
- 格式校验：仅支持 PDF/DOCX/TXT，拒绝非法格式
- 编码处理：pdfplumber + UTF-8，解决中文乱码
- 语义分块：按段落/句子拆分，保留上下文
- 重叠策略：512 tokens + 64 overlap

代码精简：从 800 行 → 300 行，删除无效注释
```

**下午：向量数据库重构**
```
改进点：
- 抽象层设计：VectorDB 基类，Milvus 实现
- 解耦：业务代码不依赖具体向量库
- 连接池：避免重复连接报错
- 功能：add、search、delete、update 完整实现

删除：Chroma 相关代码，聚焦 Milvus 做深
```

**踩坑记录**：
- 问题：初始直接调用 Chroma API，切换 Milvus 时重构大量代码
- 解决：抽取抽象接口，实现解耦

---

#### Day 3：RAG 核心 + 接口服务

**上午：RAG 全链路闭环**
```
改进点：
- Hybrid 检索：Dense 向量 + BM25 关键词，RRF 融合
- Prompt 工程：约束模型"仅基于文档回答"，减少幻觉
- 幻觉检测：文本相似度匹配，低于 50% 标记疑似幻觉
- 双模式：本地模型（量化版）+ 在线 API 兜底

技术细节：
- 本地模型：4-bit 量化 Qwen-7B，显存 4GB 可跑
- 在线 API：通义千问/文心一言，配置文件切换
- 生成参数：temperature=0.3，控制随机性
```

**下午：FastAPI 接口开发**
```
实现 4 个核心接口：
1. POST /api/v1/documents/upload - 文档上传
2. POST /api/v1/chat/ask - 问答查询
3. GET /api/v1/documents/ - 文档列表
4. DELETE /api/v1/documents/{id} - 文档删除

增强：
- 参数校验：文件类型、大小限制
- 全局异常捕获：返回标准化报错
- 跨域支持：CORS 配置
- Swagger 文档：/docs 自动生成
```

**踩坑记录**：
- 问题：大模型幻觉严重，编造答案
- 解决：Prompt 约束 + 检索置信度判断 + 相似度检测兜底

---

#### Day 4：稳定性 + 部署

**上午：稳定性精简**
```
改进点：
- 限流器：保留 Token Bucket，仅做接口限流
- 删除：熔断器（单机版意义不大）
- 新增：大模型调用超时重试（最多 2 次）
- 说明：单机版基础限流，非分布式高可用
```

**下午：Docker 部署**
```
改进点：
- Dockerfile：基于 Python 3.11 slim，轻量镜像
- docker-compose.yml：一键启动 Milvus + 项目服务
- 健康检查：Milvus 就绪后再启动项目
- 部署文档：3 步启动流程，无复杂操作

踩坑记录：
- 问题：Docker 启动后项目无法连接 Milvus
- 原因：端口映射错误、容器启动顺序
- 解决：统一端口配置、depends_on + healthcheck
```

---

#### Day 5：踩坑文档 + 代码质量

**上午：DEVELOP_PIT.md 踩坑复盘**
```
记录 7 个真实踩坑案例：
1. PDF 解析中文乱码
2. 向量检索精度过低
3. 本地大模型显存溢出
4. Milvus 容器连接失败
5. 大模型幻觉问题
6. API 跨域和参数校验
7. 技术栈选型失误（Celery → 同步任务）

每个案例包含：
- 问题场景
- 原因分析
- 解决方案（代码片段）
- 经验总结
```

**下午：代码质量整改**
```
改进点：
- 核心逻辑代码：控制在 2000 行内
- 目录结构：document/vector/llm/api/utils 清晰分层
- 注释：函数/类注释完整
- 格式化：black 统一代码风格
- 配置文件：config.py 集中管理，支持多环境
```

---

## 整改成果对比

| 维度 | v1.0（整改前） | v2.0（整改后） |
|------|---------------|---------------|
| **项目定位** | 虚假"企业级" | 诚实"求职 Demo" |
| **技术栈** | 冗余复杂（Celery/Redis/PG/K8s） | 精简可落地（SQLite/单机 Docker） |
| **核心功能** | 不完整、浅尝辄止 | 全流程闭环 |
| **代码质量** | 800+行/模块、冗余注释 | 300行/模块、结构清晰 |
| **部署难度** | 无法一键启动 | Docker Compose 一键启动 |
| **可演示性** | 差 | 完整 Demo 流程 |
| **踩坑记录** | 无 | 7 个真实案例 |
| **技术诚实** | 吹嘘生产级 | 明确说明局限 |

---

## 求职展示建议

### 演示流程（5 分钟）

1. **项目介绍**（1 分钟）
   - 这是 RAG 问答 Demo，实现文档上传→问答全流程
   - 单机版可运行，非企业级，但功能完整
   - 过程中遇到 PDF 乱码、检索精度低、幻觉等问题

2. **功能演示**（2 分钟）
   - 上传测试文档
   - 输入问题查看回答
   - 展示 Swagger 接口文档

3. **技术亮点**（1 分钟）
   - Hybrid 检索：向量 + 关键词
   - Prompt 工程：约束模型减少幻觉
   - 4-bit 量化：本地模型显存优化

4. **踩坑复盘**（1 分钟）
   - 讲 1-2 个最有代表性的问题
   - 重点讲解决过程，体现问题解决能力

### 推荐话术

> "这是一个我独立开发的 RAG Demo，实现了从文档处理到大模型生成的完整流程。
> 过程中遇到了 PDF 乱码、检索精度低、显存溢出等问题，通过语义分块、混合检索、模型量化等方法解决。
> 项目为单机版 Demo，功能完整但非企业级，技术选型上也有过度设计的教训，后续会继续优化。"

---

## 后续优化方向

1. **功能增强**
   - 多模态支持（图片、表格解析）
   - 高级幻觉检测（NLI 模型）
   - 用户反馈闭环（RLHF）

2. **性能优化**
   - 异步文档处理（如需高并发）
   - 向量库分片（数据量大时）
   - 缓存优化（Redis 引入）

3. **生产化**
   - 分布式部署（K8s）
   - 专业监控（Prometheus）
   - 多租户支持

---

## 核心收获

### 技术层面
- **简单优先**：能用 SQLite 就不用 PostgreSQL
- **可维护性 > 先进性**：代码少、依赖少、调试容易
- **诚实面对局限性**：明确说明是 Demo，而非吹嘘

### 求职层面
- **展示问题解决能力**：讲清楚问题→分析→解决
- **技术诚实是加分项**：承认不足，说明优化方向
- **可演示 > 功能多**：能跑通的 Demo 比半成品强

---

*整改完成时间：2026-04-08*  
*当前版本：v2.0 求职实战版*

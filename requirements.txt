# RAG Demo 精简依赖
# 聚焦核心功能，删除冗余组件

# Web 框架
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# RAG 框架
langchain==0.1.0
langchain-community==0.0.13
langchain-openai==0.0.5

# 向量数据库
pymilvus==2.3.6

# Embedding 模型
sentence-transformers==2.2.2

# 文档解析
pypdf==3.17.4
python-docx==1.1.0
pdfplumber==0.10.4

# 数据处理
numpy==1.26.3
pandas==2.1.4

# 文本处理
tiktoken==0.5.2
rank-bm25==0.2.2
jieba==0.42.1

# HTTP 客户端
httpx==0.26.0
aiohttp==3.9.1

# 配置管理
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0

# 数据库
sqlalchemy==2.0.25
aiosqlite==0.19.0

# 工具库
aiofiles==23.2.1
prometheus-client==0.19.0
tqdm==4.66.1
python-jose[cryptography]==3.3.0

# 注意：已删除以下冗余依赖
# - celery, redis, flower（改为同步任务）
# - psycopg2（改为 SQLite）
# - kubernetes, helm（改为单机 Docker）
